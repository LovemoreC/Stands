version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "sqlite:///app.db"
    volumes:
      - sqlite_data:/app/app.db
volumes:
  sqlite_data:
